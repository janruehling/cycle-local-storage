import { Calendar } from 'StyleFn'

const calendar = {
  name: 'Calendar',
  children: [{
    name: '',
    fn: Calendar({
      year: '2019',
      month: 'February'
    })
  }]
}

export default calendar

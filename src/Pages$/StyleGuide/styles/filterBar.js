import { div } from '@cycle/dom'

const children = [{
  name: '',
  fn: div('TODO: Finish filterBar in it\'s own function'),
  style: {
    width: '400px'
  }
}]

const filterBar = {
  name: 'Filter Bar',
  children: children,
  style: {
    width: '100%'
  }
}

export default filterBar

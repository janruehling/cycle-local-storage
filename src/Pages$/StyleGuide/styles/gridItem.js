import { GridItem } from 'StyleFn'

const children = [{
  name: '',
  fn: GridItem({
    image: null,
    icon: 'Male',
    text: 'Felicity Granville',
    size: 132
  })
}]

const gridItem = {
  name: 'GridItem',
  children: children
}

export default gridItem

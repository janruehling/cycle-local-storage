import { div } from '@cycle/dom'

const children = [{
  name: '',
  fn: div('TODO: Finish listView in it\'s own function')
}]

const listView = {
  name: 'List View',
  children: children,
  style: {
    width: '100%'
  }
}

export default listView

import { div } from '@cycle/dom'

const children = [{
  name: '',
  fn: div('TODO: Finish search in it\'s own function')
}]

const searchBar = {
  name: 'Search Bar',
  children: children,
  style: {
    width: '100%'
  }
}

export default searchBar
